library(later)
library(rsconnect)

#shiny.auto <- function(interval = 300){ # 2 hours 60 minutes 60 seconds
#  source("app.R")
#  later::later(shiny.auto, interval)
#}

#shiny.auto()


# 2° Option via restart servidor: 
# restartApp(appName = 'dashboard_eleicao_2022_r')


# 3° Opção, Stream data via reactiveValues object. 


